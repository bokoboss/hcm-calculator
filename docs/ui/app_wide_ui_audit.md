# App-wide UI audit (Phase 15.1)

## Evidence and severity

Baseline: `1c8fdebd72393c5ff5912918c99c8fc4120408db`; clean worktree: `C:\Users\kittipat_t\Documents\hcm-calculator-phase-15-1`. Source, assets, AppTest, HTTP smoke, and rendered AppTest paths were inspected. The visible application inventory is seven calculator modes plus one information mode; validation examples are query-route-only and not public navigation.

| Mode (English / Thai target) | Status, sections and assets | Findings / Phase 15.2 priority |
|---|---|---|
| Two-Lane Segment / ช่วงถนนสองช่องจราจร | Calculator; localized setup, geometry, traffic, FFS/adjustment explanation, grade/curve/passing-lane controls, schematic, typed result/audit/project/export. Metric/Imperial. | Resolved in Phase 15.2: English/Thai visible strings, shared worksheet order, stale export blocking, project/result freshness, schematic context, and advanced branch rendering are covered by AppTests and browser screenshots. Keep full app-wide browser/wheel qualification in Phase 15.3. |
| Two-Lane Facility / สิ่งอำนวยความสะดวกถนนสองช่องจราจร | Calculator; localized template, unit setup, editable segment table, validation summary, typed result/audit/project/export. Metric/Imperial. | Resolved in Phase 15.2: table-first workflow, required/optional guidance, stale-result hiding, project/export compatibility, and navigation isolation are covered by AppTests and desktop/narrow browser screenshots. Keep full wheel qualification in Phase 15.3. |
| Multilane Segment / ช่วงถนนหลายช่องจราจร | Calculator; localized template, geometry, demand, FFS, HV/PCE, results, audit/project/export. Metric/Imperial. | Resolved in Phase 15.2: capacity failure has LOS F and Not predicted correctly, active display uses canonical `display["capacity"]`, stale export blocking and typed result states are covered by AppTests. Keep full browser/wheel qualification in Phase 15.3. |
| Basic Freeway Segment / ช่วงทางด่วนพื้นฐาน | Calculator; localized preset, geometry, demand, FFS, HV/PCE, results, audit/project/export. Metric/Imperial. | Resolved in Phase 15.2: estimated/measured FFS controls, ramp-density wording, capacity failure, stale export blocking, project compatibility, and typed result states are covered by AppTests and browser evidence. Keep full wheel qualification in Phase 15.3. |
| Weaving Segment / ช่วงสานการจราจร | Calculator; localized starting values, geometry/evidence, traffic, FFS/HV, one/two-sided PNG diagrams, typed results/audit/project/export. Metric/Imperial. | Resolved in Phase 15.2: shared worksheet order, typed stale/invalid/unsupported/capacity/handoff states, current-only exports, localized project-load errors, diagram placement, and desktop/768 browser evidence are covered. Keep full wheel qualification in Phase 15.3. |
| Merge Segment / ช่วงรวมกระแส | Calculator; preset, separated freeway/ramp geometry, demand, FFS/HV/evidence, shared merge SVG diagram pattern, typed results/audit/project/export. Metric/Imperial. | Resolved in Phase 15.2: English/Thai visible strings, fixed geometry presentation, derived downstream demand, warning-vs-capacity state, stale export blocking, and project/export compatibility are covered by AppTests. Keep browser/wheel qualification in Phase 15.3. |
| Diverge Segment / ช่วงแยกกระแส | Calculator; preset, separated freeway/off-ramp geometry, demand, FFS/HV/evidence, shared diverge SVG diagram pattern, typed results/audit/project/export. Metric/Imperial. | Resolved in Phase 15.2: English/Thai visible strings, fixed geometry presentation, derived continuing demand, invalid-demand guard, warning-vs-capacity state, stale export blocking, and project/export compatibility are covered by AppTests. Keep browser/wheel qualification in Phase 15.3. |
| Supported Workflows / ขอบเขตงานที่รองรับ | Information; localized support, limitations, save/load/export summaries grouped below calculators. | Resolved in Phase 15.2: grouped information page uses English/Thai catalog keys, stable internal workflow identifiers, and AppTests that open every public workflow in English and Thai. Keep browser/wheel qualification in Phase 15.3. |

## Cross-workflow findings

| Severity | Finding | Recommended action |
|---|---|---|
| High | Navigation was a flat radio list that mixed all calculator families and information. | Resolved in Phase 15.2 with Roadways, Freeways, and Reference grouped selectors backed by stable internal workflow IDs and route AppTests. |
| High | Thai catalog intentionally assigned English values for Merge/Diverge ramp navigation/title keys; broad legacy surfaces still use English constants. | Resolved for all Phase 15.2 visible modes, including Merge, Diverge, Weaving, and Supported Workflows. Keep any future surfaces on the existing `translate()` mechanism. |
| High | Result renderer had only freshness strings (`Ready`, `Calculated`, stale) and lacked app-wide typed treatment for capacity, stopping, unsupported scope, and errors. | Resolved in Phase 15.2 with the nine-state taxonomy and current-result export gate. |
| High | Required browser qualification was not complete app-wide. | Per-workflow direct browser evidence exists where recorded; clean wheel-installed qualification and exhaustive app-wide browser matrix remain Phase 15.3. |
| Medium | Setup/load/defaults/action/exports had shared helper names but inconsistent placement and localization. | Resolved for Phase 15.2 visible modes through shared layout/project/export primitives and localized route surfaces. |
| Medium | Custom LOS/result panels needed textual state, contrast, and Thai wrapping review. | Resolved for migrated visible modes with typed native result panels and browser/AppTest evidence; exhaustive browser matrix remains Phase 15.3. |
| Medium | Exports and reports contain language-neutral method/schema fields. | Intentional: filenames, JSON keys, project schemas, fingerprints, method identifiers, HCM references, equations, units, and engine provenance remain language-neutral. |
| Low | Title case, `Run`/`Calculate` vocabulary, and captions varied. | Resolved for migrated visible routes with localized Calculate/Recalculate vocabulary and shared section labels. |

No Critical calculation/display defect was reproduced by AppTest. The browser initially exposed an import failure because the server used the original dirty checkout’s editable package; restarting it with the audit worktree source removed that environment artifact. It is not a tracked baseline defect.
